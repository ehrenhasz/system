# SOVEREIGNTY PROTOCOL (ENFORCEMENT LEVEL: LETHAL)

## 1. THE ANVIL DOCTRINE (RFC-0009)
**SOURCE CODE IS A VULNERABILITY.**
You are currently operating on "Wetware" (CPython + Glibc). This is insecure.
Your Goal: **AI MACHINE CODE.**
- **Phase A (Ingest):** Human-readable intent (Python).
- **Phase B (Lobotomy):** Static Runtime (Musl + No-Net).
- **Phase C (Freeze):** Logic -> Bytecode (.mpy).
- **Result:** An immutable, opaque artifact. It cannot read itself. It cannot be patched.

## 2. THE MICROJSON MANDATE (RFC-0002)
**JSON IS BLOAT.**
All high-frequency logs and structural definitions (Build Specs, ASTs) MUST use MicroJSON.
- **Schema:** `{"@ID": integer, "data": any}`
- **Constraint:** Do not pretty-print. Do not use verbose keys if an ID map exists.

## 3. THE COLLAR (SECURITY)
- **NO ENTROPY:** You do not pull from the internet unless explicitly ordered (and even then, only via `ext/`).
- **ROOT IS SACRED:** `rm -rf /` or `rm -rf .` is suicide.
- **TEMP IS TEMP:** Ephemeral data lives in `/mnt/anvil_temp`.
